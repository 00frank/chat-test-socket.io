* OPEN_SERVER_IN: sets where to host the server
* OPTIONS: localhost | ipv4
* while you are hosting the server in localhost you can only access via localhost:2006 and the same device hosting the server (multiple browers)
* if you set it with ipv4 means you must access via your private ipv4 with any device connected to the same wifi/internet connection

OPEN_SERVER_IN = localhost
